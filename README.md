# FuzzyMatchTool

## **Принцип работы**

Приложение использует **нечеткое сравнение строк** по методу Левенштейна¹ — алгоритм, который измеряет «расстояние» между двумя строками, подсчитывая **минимальное количество операций** (вставка, удаление, замена символа), необходимое для превращения одной строки в другую.

### **Пример: Как считается схожесть**

Возьмем для примера слова **"конь"** и **"кот"**:
    
```python
"конь" → "коть"  # Замена "н" на "т"
"коть" → "кот"   # Удаление "ь"
```

**Результат:** потребовалось **2 операции**. Расстояние Левенштейна = 2.

**Формула расчета схожести в процентах:**
> `Схожесть (%) = (1 - Расстояние_Левенштейна / max(Длина_Строки1, Длина_Строки2)) × 100%`

**Для нашего примера:**
> `Схожесть = (1 - 2 / 4) × 100% = 50%`

*где 4 — длина более длинного слова ("конь")*

**Вывод:** слова "конь" и "кот" имеют **50% схожести** по этому методу.

## **Применение**

В учетных системах разных компаний одни и те же объекты часто называются по-разному. Например:

- `"шрот подсолнечный B-50"` и `"Шрот подсол. B50"`
- `"ООО Рыба-Мясо Финанс УК"` и `"УК ООО Рыба Мясо Финанс"`

Такие различия затрудняют поиск и консолидацию данных.

**✅ Решение:** Это приложение помогает быстро создать справочник для сопоставления (меппинга) таких строк, который можно использовать в сводных отчетах по группе компаний.

## **Пошаговая инструкция**

### **1. Подготовка данных**
Нажмите **"📋 Открыть исходные данные"**, чтобы создать и открыть файл для редактирования, который уже будет заполнен примером в качестве образца:

|data1           |data2                          |
|----------------|-------------------------------|
|Магнолия|АО «Белоусова Одинцова»|
|Константинова и партнеры|ОАО «Носкова-Козлов»|
|РАО «Лихачева Тарасов»|ООО «Дорофеева Богданов»|
|...|...|


**Важно:**
- Скопируйте в столбцы data1 и data2 свои значения для сравнения.
- Для каждого значения из столбца data1 будут подобраны похожие значения из столбца data2.
- **Не забудьте сохранить файл** после заполнения.

### **2. Установка критерия схожести**
Нажмите **"🔧 Установить уровень схожести"** и установите нужный процент (от 10 до 99):

- **Высокий уровень (90-99%)** — практически идентичные строки, меньше совпадений, но более точные.
- **Низкий уровень (менее 90%)** — больше совпадений, но возможны неточности.

### **3. Запуск сопоставления**
Нажмите **"🔍 Найти схожие значения"** для запуска поиска совпадений.

### **4. Результаты**
После обработки откроется файл с найденными совпадениями.

**💡 Рекомендация:** Запускайте сравнение несколько раз, меняя критерий схожести, чтобы найти оптимальный баланс между количеством и точностью совпадений. Используйте опции из меню Настройки для уточнения результатов сравнения 👇 

## **Настройки**
- **Нормализация слов**: приводит слова к начальной форме для лучшего сравнения.  
  *Пример: `"бежал" → "бежать", "стола" → "стол"`*

- **Учитывать порядок слов**: при включении порядок слов влияет на схожесть.  
  *Пример: `"сладкий апельсин"` и `"апельсин сладкий"` считаются разными*

- **Учитывать цифры**: числовые значения влияют на сравнение.  
  *Пример: `"Здание №3, площадь 3044,4"` и `"Здание №6, площадь 5040,9"` не похожи*

- **Учитывать юридические формы**: сокращения организаций влияют на схожесть.  
  *Пример: `"ЗАО Восход"` и `"ООО Восход"` считаются разными компаниями*

## **Прочее**

Перед сравнением текст проходит предварительную обработку, а именно:
    
- очистка от лишних пробелов, знаков препинания, стоп-слов;
- приведение к нижнему регистру;
---

*¹ Владимир Иосифович Левенштейн (1935–2017) — советский и российский математик, специалист в области теории информации и кодирования.*

## Установка

1. Скопируйте репозиторий:
```bash
git clone https://github.com/kaetosh/Gantt.git
```
2. Установите необходимые библиотеки:
```bash
pip install -r requirements.txt
```
3. Создайте .exe файл для удобного использования:
```bash
pyinstaller --onefile main.py
```

## Лицензия
Этот проект лицензирован под MIT License.

## Контакты
Если у вас есть вопросы или предложения, не стесняйтесь обращаться:
Александр

me@karabedyan.ru

https://github.com/kaetosh

Спасибо за использование FuzzyMatchTool! Надеемся, он поможет вам в работе над отчетами!
